export declare class AiCreditsService {
    private static getHeaders;
    static generateLeadScore(leadData: any, conversationHistory: any[]): Promise<any>;
    static summarizeConversation(conversationHistory: any[]): Promise<any>;
}
